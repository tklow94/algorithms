//accepts a key
//hashes the key
//Retreives the key-value pair in the hash table
    //remeber it is in a nested array, so there may be multiple values, and we need to make sure by looping we get the right one.
//if the key isn;t found returns undefined